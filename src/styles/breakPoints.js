const size = {
    xs: '450px',
    sm: '730px',
    md: '915px',
    lg: '1050px',
    xl: '1260px',
    xll: '1345px'
}

const breakpoints = {
    xs: `(max-width: ${size.xs})`,
    sm: `(max-width: ${size.sm})`,
    md: `(max-width: ${size.md})`,
    lg: `(max-width: ${size.lg})`,
    xl: `(max-width: ${size.xl})`,
    xll: `(max-width: ${size.xll})`
}

export default breakpoints